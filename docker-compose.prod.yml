# ==============================================================================
# Docker Compose - Production Configuration
# ==============================================================================
# Production deployment with optimized settings and optional Nginx reverse proxy.
#
# Usage:
#   Build & Start:  docker-compose -f docker-compose.prod.yml up --build -d
#   Stop:           docker-compose -f docker-compose.prod.yml down
#   Logs:           docker-compose -f docker-compose.prod.yml logs -f
#   Rebuild:        docker-compose -f docker-compose.prod.yml build --no-cache
#
# Environment:
#   Create .env file with all required variables (copy from .env.example)
# ==============================================================================

services:
  # ----------------------------------------------------------------------------
  # Next.js Production Application
  # ----------------------------------------------------------------------------
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
      args:
        # =====================================================================
        # Build-time variables (NEXT_PUBLIC_* are baked into client bundle)
        # These are visible to browser - NEVER put secrets here!
        # =====================================================================

        # Application URL
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}

        # Auth configuration (visible to browser for UI decisions)
        NEXT_PUBLIC_AUTH_PROVIDER: ${NEXT_PUBLIC_AUTH_PROVIDER:-local}
        NEXT_PUBLIC_AUTH_COOKIE_NAME: ${NEXT_PUBLIC_AUTH_COOKIE_NAME:-rediver_auth_token}
        NEXT_PUBLIC_REFRESH_COOKIE_NAME: ${NEXT_PUBLIC_REFRESH_COOKIE_NAME:-rediver_refresh_token}
        NEXT_PUBLIC_OAUTH_CALLBACK_URL: ${NEXT_PUBLIC_OAUTH_CALLBACK_URL:-}

        # Keycloak (only if using OIDC auth)
        NEXT_PUBLIC_KEYCLOAK_URL: ${NEXT_PUBLIC_KEYCLOAK_URL:-}
        NEXT_PUBLIC_KEYCLOAK_REALM: ${NEXT_PUBLIC_KEYCLOAK_REALM:-}
        NEXT_PUBLIC_KEYCLOAK_CLIENT_ID: ${NEXT_PUBLIC_KEYCLOAK_CLIENT_ID:-}
        NEXT_PUBLIC_KEYCLOAK_REDIRECT_URI: ${NEXT_PUBLIC_KEYCLOAK_REDIRECT_URI:-}

        # Monitoring (DSN is safe to expose - not a secret)
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}

    container_name: rediver-ui-prod

    # =========================================================================
    # Runtime environment variables (server-side only, NOT in browser)
    # These are NEVER sent to browser - only available on Next.js server
    # =========================================================================
    environment:
      NODE_ENV: production

      # Internal backend URL - used by Next.js server to proxy requests
      # This is HIDDEN from browser - only server can see this
      # In Docker: use service name (e.g., http://api:8080)
      # Standalone: use internal network URL
      BACKEND_API_URL: ${BACKEND_API_URL:-http://api:8080}
      API_TIMEOUT: ${API_TIMEOUT:-30000}

      # Security secrets - MUST be server-only
      CSRF_SECRET: ${CSRF_SECRET}
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET:-}

      # Cookie settings
      SECURE_COOKIES: ${SECURE_COOKIES:-true}
      COOKIE_MAX_AGE: ${COOKIE_MAX_AGE:-604800}

      # Token refresh
      ENABLE_TOKEN_REFRESH: ${ENABLE_TOKEN_REFRESH:-true}
      TOKEN_REFRESH_BEFORE_EXPIRY: ${TOKEN_REFRESH_BEFORE_EXPIRY:-300}

    # Port mapping
    ports:
      - "3000:3000"

    # Health check
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

    # Restart policy
    restart: unless-stopped

    # Network
    networks:
      - app-network

    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M

    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

# ==============================================================================
# Nginx Reverse Proxy (Optional - uncomment to use)
# ==============================================================================
# nginx:
#   image: nginx:alpine
#   container_name: rediver-nginx
#
#   ports:
#     - "80:80"
#     - "443:443"
#
#   volumes:
#     - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
#     - ./nginx/ssl:/etc/nginx/ssl:ro
#
#   depends_on:
#     nextjs:
#       condition: service_healthy
#
#   healthcheck:
#     test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/api/health"]
#     interval: 30s
#     timeout: 10s
#     retries: 3
#     start_period: 30s
#
#   restart: unless-stopped
#
#   networks:
#     - app-network
#
#   deploy:
#     resources:
#       limits:
#         cpus: '0.5'
#         memory: 256M
#       reservations:
#         cpus: '0.1'
#         memory: 64M
#
#   logging:
#     driver: "json-file"
#     options:
#       max-size: "10m"
#       max-file: "3"

# ==============================================================================
# Networks
# ==============================================================================
networks:
  app-network:
    driver: bridge
